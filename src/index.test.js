import expect from 'expect';

describe('Our first test', () => {
  it('should pass', () => {
    expect(true).toEqual(true);
  });
    it('should fail', () => {
    expect(true).toEqual(true);
  });
});